#include<stdio.h>
#include<string.h>
int main(){
    char arr[20];
    fgets(arr,sizeof(arr),stdin);
    int count=0;
    for(int i=0;i<arr[i]!=0;i++){
        if(arr[i]>='A' && arr[i]<='Z'){
            count++;
           
        }}
        if(count>0){
             printf("Valid");
        }
        else{
            printf("Invalid");
        }
 }
   
    
